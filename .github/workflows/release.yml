name: Release to npm

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 1.0.0 or major/minor/patch)'
        required: false

permissions:
  contents: write
  id-token: write # Required for npm trusted publishing (OIDC)
  issues: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    name: Release

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org/'
          cache: 'yarn'

      - name: Update npm (trusted publishing requires npm >= 11.5.1)
        run: npm install -g npm@latest

      - name: Install dependencies
        run: yarn install

      - name: Run linter
        run: yarn lint

      - name: Run type check
        run: yarn typecheck

      - name: Run tests
        run: yarn test:coverage

      - name: Build library
        run: yarn prepare

      - name: Configure Git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Release (auto-detect version)
        if: github.event_name == 'push'
        run: npm run release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release (manual version)
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.version
        run: npm run release -- --increment=${{ github.event.inputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release (no version bump)
        if: github.event_name == 'workflow_dispatch' && !github.event.inputs.version
        run: npm run release -- --no-increment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
